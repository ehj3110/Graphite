# Extrusion woodpile phase scripts (archived)

Development spike scripts from the P0–P4 extrusion validation pass (Jul 2026).

**Production path:** use `scripts/generate_piecewise_woodpile.py` with `--generator extrude` (default).

These scripts remain for regression reference and slice PNG comparisons under `outputs/explicit_woodpile/P0` … `P4`. Re-run from repo root only if needed for debugging.
